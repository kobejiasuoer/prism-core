[project]
name = "prism-core"
version = "0.1.0"
description = "AI-native investment research workflow framework"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
